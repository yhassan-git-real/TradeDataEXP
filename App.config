<?xml version="1.0" encoding="utf-8" ?>
<configuration>
    <!-- Note: Connection strings are now managed through .env file via ConfigurationService -->
    <!-- This App.config is kept for potential framework compatibility requirements -->
    <connectionStrings>
        <!-- Connection string is dynamically loaded from .env file -->
    </connectionStrings>
    
    <appSettings>
        <!-- Application settings are now managed through .env file via ConfigurationService -->
    </appSettings>
</configuration>
